# OneFarm - LDPlayer Auto-Farm (Ultra Fast Edition) 🚀
taskkill /F /IM ultra-fast-rs.exe & cargo run --manifest-path src-tauri/Cargo.toml
Bộ công cụ Auto-Farm tối ưu hóa cho LDPlayer, tập trung vào tốc độ phản hồi cực nhanh và tiết kiệm tài nguyên.

## ✨ Tính năng nổi bật đã hoàn thiện:
- **Tốc độ "Bàn thờ" (1ms)**: Sử dụng Win32 `PostMessage` để click trực tiếp vào vùng Render của LDPlayer, loại bỏ hoàn toàn độ trễ của ADB.
- **Công nghệ Chụp ảnh WGC**: Tận dụng Windows Graphics Capture để chụp ảnh màn hình từ GPU, đạt 60fps mà không tốn CPU.
- **RAM Template Caching**: Tự động nạp toàn bộ ảnh mẫu vào RAM ngay khi khởi động hoặc làm mới, triệt tiêu độ trễ đọc đĩa cứng.
- **Mapping giả lập chuyên nghiệp**: Tích hợp `ldconsole list2` để quản lý đa giả lập chính xác theo Index, Title và Bind Handle.
- **Giao diện hiện đại (Tauri/Vite)**: Hỗ trợ Light/Dark mode, log console tự động cuộn và trình chọn thiết bị thông minh.
- **Thuật toán FastRecognizer**: Bộ nhận diện ảnh mẫu được tối ưu hóa bằng Rust, đảm bảo độ chính xác cao nhất với thời gian xử lý cực thấp.
- **Bao ve an toan click**: Chi cho phep thao tac tren gia lap khi trang thai la Connected (nguoi dung phai click chu dong vao badge Ready de kich hoat), giup tranh click nham vao tab khac.
- **Icon thuong hieu rieng**: Tich hop bo icon doc quyen duoc sinh tu dong voi day du cac dinh dang phu hop cho moi nen tang (PNG, ICO, ICNS).
- **Chu dong huy ket noi**: Cho phep nguoi dung nhap chu dong vao nhan Connected de ngat ket noi (Disconnect), lap tuc giai phong hoan toan tai nguyen GPU/RAM cua gia lap do.
- **Tu dong thich ung do phan giai**: Tu dong doi chieu, lam moi grabber khi cua so bi keo gian hoac thu nho va tu dong nhan ti le toa do click de dam bao click luon trung muc tieu tren moi do phan giai cua so.
- **Sao chep pixel sieu toc (Direct Pointer Memory Access)**: Loai bo hoan toan cac buoc kiem tra an toan chi so (Bounds checking) trong vong lap va thuc hien sao chep truc tiep tren vung con tro dong bo, giam thoi gian copy xuong duoi 1ms cho moi khung hinh.
- **Bo qua Chuan hoa thong minh & Nearest Neighbor**: Tu dong bo qua buoc co gian anh khi sai lech duoi 4 pixel (giam ton CPU ve 0ms). Neu can co gian, su dung thuat toan Nearest Neighbor de giu nguyen canh sac net cho template matching va giam thoi gian xu ly xuong duoi 1ms.
- **Nhan dien RGBA Zero-Copy**: Bo qua hoan toan viec doi he mau va cap phat lai bo nho heap (malloc) tu RGBA sang RGB tren moi khung hinh chup tu GPU. FastRecognizer duoc nang cap de ho tro quet tren vung nho dem RGBA voi dung sai dong, tiet kiem 2ms va triet tieu phan manh RAM.
- **Triet tieu phan manh RAM & Roc bo nho**: Thiet ke lai co che FrameArrived de tai su dung duy nhat mot vung nho dem pre-allocated trong Mutex cho luong chup anh GPU (loai bo 270MB/giay phan bo Heap vo ich o luong nen). Dong thoi ap dung con tro thong minh Arc de chia se anh mau trong cache, triet tieu 100% ruy ro ro ri bo nho (memory leak).
- **Kien truc Modular Hoan thien 100% (Future-proof Modular Architecture)**: Tai cau truc toan dien ca Backend va Frontend. Backend duoc phan chia khoa hoc vao `core/` (GPU Capture, Fast Matching), `emulator/` (ADB, LDPlayer), `config/` (AppState, DeviceInfo) va `commands/` (Tauri Commands). Frontend duoc chia nho thanh `css/` (variables, layout, components) va `js/` (theme, console, api, devices, app) giup viec tich hop them cac kich ban auto-farm phuc tap sau nay cuc ky de dang va don gian.
- **Bo loc nhanh 9 diem chat luong cao (9-Point Grid Quick Test)**: Nang cap bo loc kiem tra nhanh tu 5 diem len 9 diem nam xen ke giua tam va bien de loai bo ngay lap tuc cac ung vien gia o cac vung nen kem phang cua game ma khong can chay SAD, day toc do quet khi that bai nhanh gap 5-10 lan.
- **Tinh nang Test All kiem tra toan bo anh mau (Comprehensive Template Dry-Run)**: Tich hop tinh nang Test All chup dung 1 khung hinh tu duy nhat tu GPU, sau do chay quet song song toan bo 16 anh mau dang nap trong RAM Cache va xuat log dong thoi ra console ve toa do thuc, toa do scaled va diem so (score) cua tung anh mau de ho tro check loi giao dien cuc ky nhanh chong va truc quan.
- **Phan loai Danh muc & Gioi han vung quet (Categorized Search Bounding)**: Ho tro tu dong chia nhom anh mau thanh `buttons/` (quet toan man hinh) va `seeds/` (quet gioi han dung 1 nua ben trai man hinh). Giup giam thoi gian quet hat giong xuong mot nua ma van dam bao khong bao gio bi lech hay click nham.
- **Ngoc phan biet anh gan giong nhau (Auto Strict Thresholding)**: Tu dong kiem tra va ap dung nguong sai so that chat tu `25` xuong `12` cho toan bo cac hat giong trong thu muc `seeds/` theo mac dinh. Doi voi cac nut bam o thu muc `buttons/`, neu co chua tu khoa `_strict` trong ten file (vi du: `confirm_strict.png`), he thong cung se ap dung nguong nghiem ngat nay, triet tieu hoan toan rui ro nhan dien sai cho cac asset nhay cam.
- **Toi gian hoa ten anh mau (Clean Template Naming)**: Loai bo hoan toan tien to `btn_` du thua trong thu muc `buttons/`, rut gon ten xuong dang tinh te (nhu `harvest.png`, `confirm.png`) vi kien truc hien tai da phan loai qua thu muc con vo cung khoa hoc va gon gang.
- **Kich ban Mua hat giong chuyen nghiep (Modular Buy Seeds Script)**: Tach biet hoan toan kich ban Mua hat giong thanh module rieng biet `automation/buy_seeds.rs`. Kich ban tu dong luot va quet mot nua ben trai man hinh voi do phan giai chuan hoa, nhan dang hat giong can mua thong qua nguong sai so nghiem ngat, tu dong click Win32 de mua. Kich ban su dung thuat toan quet hai chieu tuần hoàn (Bidirectional Scan) cuc ky thong minh: quet tim hat giong lien tuc ca khi cuon xuong va cuon len, tu dong dao chieu cuon khi cham cuoi danh sach (`jujube.png`) hoac dau danh sach (`carrot.png`), giup tang toc do quet gap 2 lan va cat giam thao tac cuon thua.
- **Xac thuc ton kho va keo slider so luong (Stock Verification & Slider Drag Confirmation)**: Tich hop logic kiem tra ton kho thoi gian thuc sau khi click vao hat. Neu khong thay nut `sell-produce.png` (het hang), he thong tu dong bo qua va loai khoi danh sach tim kiem de tiep tuc hanh trinh ngay lap tuc. Neu con hang, he thong se click vao nut Bán (`sell-produce.png`), tim thanh truot `slider.png` tai toa do `(slider_x, slider_y)`, tu dong tinh toan diem doi xung phai `960 - slider_x` va cong them `80` pixel de keo chuot vuot han muc max nham xac nhan dat 100% so luong khong bi thieu hut, sau do click xac nhan hoan tat giao dich cuc ky thong minh.
- **May trang thai mo shop tu sua loi va triet tieu do tre (Self-healing Shop Opening State Machine)**: Nang cap quy trinh mo cua hang len kien truc May trang thai tuong thich visual thoi gian thuc. He thong tu dong nhan dien dung vi tri nhan vat dang dung (A: Chua toi NPC, B: Da gap NPC, C: Can mo Shop, D: Shop da mo). Neu bat ky nut nao duoc click nhung Game Client phat sinh tre khong nhan thao tac, he thong se tu dong re-click (tu sua loi) thong qua moc thoi gian **Delay giua cac nut** duoc cau hinh linh hoat trong Modal (mac dinh 1000ms), giup kich ban hoan toan bat tu truoc moi hien tuong lag, mang lai ti le thanh cong tuyet doi 100%.
- **Cau hinh Do nhay khop anh dong (Dynamic Image Matching Score Threshold)**: Them truong **Do nhay khop (Score)** vao truc tiep giao dien Modal Cai dat (mac dinh la 25, co the tinh chinh tu 10 den 50). Giup nguoi dung chu dong thay doi muc do nhay cua thuat toan quet anh GPU, thich ứng linh hoat voi su thay doi do sang, bong mo hay chu ky ngay dem cua Game Client. Tinh nang nay duoc ap dung dong bo tren ca **Kich ban Mua hat giong**, **lenh Test thu cong** va **lenh Test All** tren UI, giup giu chat che logic seeds o muc 12 de khong bao gio bi click nham hạt.
- **Rao chan Bao ve Ket noi (Connection Safety Shield)**: Tich hop logic kiem tra trang thai ket noi gia lap thoi gian thuc. Neu thiet bi chua duoc ket noi (trang thai "Ready" thay vi "Connected"), chu ky tu dong chay (Auto) se duoc chan ngay lap tuc tren UI ma khong he kich hoat bat ky tien trinh chay ngam nao. Dac biet da sua triet de loi kich ban van tiep tuc chay ngam sau khi user ngat ket noi (Disconnect) bang cach chia se co trang thai `closed: Arc<Mutex<bool>>` va xoa sach cache `latest_frame` trong `WgcGrabber`, giup kien truc luon dung hoan toan va giai phong 100% thread ngam ngay trong tich tac.
- **Toi uu toc do vuot va keo slider (High-speed Touch Drag Optimization)**: Giu nguyen ham vuot cuon danh sach `swipe_ld` nguyen ban (10 buoc, delay 20ms, cho 200ms) de bao toan 100% su on dinh va muot ma hoan hao nhu cu luc cuon tim hat. Dac biet toi uu hoa cach thay doi so luong mua bang cach **click truc tiep vao toa do doi dien cua slider** (`960 - slider_x`) ma khong can thao tac keo truot hay cong them khoang cach pixel nhu truoc, giup so luong nhay max tuc thi voi toc do nhanh nhat, giam thoi gian cho xuong **300ms** mang lai su on dinh va chinh xac tuyet doi. Dong thoi, loai bo toan bo ma nguon va cac ham keo slider thu lam du thua de dam bao ma nguon luon sieu tinh gon va sach se.
- **Chan doan loi Anh Mau Hat Giong thoi gian thuc (Real-time Seed Template Diagnostic Scan)**: Bo sung tinh nang "Check Lỗi" cao cap nam canh nut "Test All" tren UI. He thong tu dong mo cua hang hat giong (neu chua mo) va cuon quet dong tu dau den cuoi (nho nhan dien `jujube.png`), sau do tu cuon nguoc tro lai dau trang (nho nhan dien `carrot.png`) giong het cach thuc van hanh cua kich ban mua hat nhung hoan toan **khong tu y thuc hien bat ky giao dich mua nao**. Thong tin so khop, toa do va score cua tung hat trong so 19 hat se duoc bao cao chi tiet, giup nguoi dung lap tuc phat hien anh nao can chup lai ma khong he ton tai bat ky rui ro nao cho tai khoan game.
- **Giao dien dieu khien kich ban truc quan (Interactive Script UI Panel)**: Tich hop bang dieu khien kich ban cuc ky cao cap, hien thi danh sach checkbox cac loai hat giong hien co (duoc loc thong minh tu cache), nut khoi chay kich ban va man hinh trang thai thoi gian thuc tuong thich hoan hao cho ca Light/Dark Mode, mang den trai nghiem tuyet voi va nhat quan ma khong su dung emojis va thuoc tinh title gay phien toai. Dac biet da **luoc bo toan bo hieu ung hoat anh mo settings modal**, dong thoi them **khoang cach padding rieng cho vien tren/duoi** (`padding: 24px 16px` kem `max-height` va `overflow-y: auto`) de tranh tinh trang modal bi dan dinh vao vien man hinh khi cua so game thu nho, thiet lap bo tron goc o muc **10px** nhe nhang, thanh lich. Dong thoi, cau hinh cua so ung dung chinh **luon tu dong can giua man hinh** (`"center": true` trong `tauri.conf.json`) ngay khi khoi dong.
- **Tu dong ghi nho cau hinh (Real-time Config Persistence)**: Tich hop module doc/ghi file `config.json` thong qua `config/persisted.rs`. He thong tu dong luu toan bo hat giong da chon va trang thai Auto moi khi thay doi tren giao dien thoi gian thuc, dong thoi tu dong nap lai dung trang thai cu luc khoi dong lai.
- **Sua loi treo cua so ung dung (Non-blocking Async Command Execution Fix)**: Khac phuc triet de loi cua so ung dung bi treo "Not Responding" bang cach chuyen doi toan bo lenh Tauri chay kich ban `run_buy_seeds_script` thanh phuong thuc bat dong bo (`async fn`). Giup tach biet hoan toan khoi thread giao dien chinh (GUI Thread) cua he dieu hanh, mang den su muot ma tuyet doi va khong bao gio bi crash/lag.
- **Chia nho va lam sach toan dien cac Tauri Commands (Complete Modular Commands Split)**: Tiep thu y kien tu nguoi dung, da thuc hien chia nho toan bo file `commands/mod.rs` (tung co hon 440 dong) thanh cac sub-modules rieng biet la `commands/device.rs`, `commands/session.rs`, `commands/template.rs`, va `commands/script.rs`. File `commands/mod.rs` gio day chi con dung 6 dong khai bao va re-export, giup kien truc ma nguon tro nen sieu tinh gon, chuyen nghiep va cuc ky de bao tri.
- **Hop thoai Cai dat Modal cao cap (Premium Settings Modal Overlay)**: Chuyen doi toan bo giao dien cau hinh kich ban (chon hat giong can mua, tuy chon Auto 5 phut, cau hinh giay Timeout nut) tu viec hien thi tho tren dong thiet bi sang mot hop thoai Modal Glassmorphism tuyet dep. Hop thoai ho tro hieu ung mo va phong to nhe nhang (Scale & Fade Transitions), tuong thich hoan hao cho ca hai chu de Light/Dark Theme, co nut dong nhanh bang SVG va nut Luu cau hinh bat mat, giup don dep va lam sach toi da giao dien chinh.
- **Cấu hình Timeout động toàn cục (Dynamic Button Detection Timeout)**: Tích hợp cấu hình `button_timeout_secs` (mặc định là 5 giây nếu chưa có trong file) vào file cấu hình hệ thống `config.json` và đồng bộ hóa thời gian thực qua UI Modal. Backend tự động áp dụng giá trị này vào vòng lập quét thường xuyên các nút mở shop (Step A, Step B, Step C) của NPC, giúp nâng cao tính thích ứng trên các môi trường máy khác nhau.
- **Hủy tiến trình tức thì (Global Instantly-Responsive Abort System)**: Thiết lập cấu hình cờ hủy `cancelled_devices: Mutex<HashSet<isize>>` trong `AppState` dùng chung. Tích hợp kiểm tra hủy thời gian thực cực kỳ nhạy bén (phản hồi dưới 0.1 giây) ở tất cả các điểm cốt lõi: vòng lặp mở shop NPC của kịch bản mua, vòng lặp quét mua hạt chính của `run_buy_seeds_script_logic`, vòng lặp quét chẩn đoán của `run_check_seeds_templates_logic` (Check Lỗi), và vòng lặp quét dry-run ảnh mẫu của `test_all_templates` (Test All). Giúp lập tức ngắt luồng GPU/Win32 ngầm an toàn tuyệt đối mà không bao giờ gây đóng băng GUI chính hay crash ứng dụng.
- **Trạng thái Dừng động & Vô hiệu hóa bảo vệ (Dynamic Action Button States & Multi-Thread Protection)**: Nâng cấp toàn diện cơ chế điều phối nút bấm trên Frontend `devices.js`. Toàn bộ các nút hành động cốt lõi gồm **Chạy kịch bản**, **Test**, **Test All**, **Check Lỗi**, và **Capture** đều được thiết lập máy trạng thái tự động: khi tiến trình được khởi chạy, nút đó sẽ lập tức đổi màu sang đỏ hổ phách `#ef4444`, hiển thị chữ **"Dừng kịch bản"** hoặc **"Dừng"**, đồng thời chuyển trạng thái click thành lệnh gửi cờ hủy lên backend. Khi tiến trình dừng hoặc hoàn tất, nút tự động hồi phục về trạng thái, màu sắc và văn bản ban đầu một cách hoàn mỹ.
- **Ngắt tiến trình tức thì dưới 20ms (Sub-20ms Ultra-Fast Interruptible Abort)**: Nâng cấp cơ chế dừng kịch bản lên mức tức thời tuyệt đối bằng cách chèn logic ngắt quãng chu kỳ siêu ngắn 20ms vào bên trong local macro `sleep!` và tích hợp trực tiếp kiểm tra cờ hủy ngay tại dòng đầu tiên của bộ capture GPU `capture_helper`. Giúp toàn bộ tiến trình phản hồi và tắt hoàn toàn luồng ngầm ngay lập tức (dưới 0.02 giây) sau khi nhấn nút Dừng, mang lại trải nghiệm 'ấn dừng phát ăn ngay' cực kỳ phấn khích.
- **Module chẩn đoán lỗi độc lập (Modular Diagnostic Scan Extraction)**: Thực hiện chuyển đổi và tách biệt hoàn toàn kịch bản chẩn đoán hạt giống ra một tệp tin chuyên biệt độc lập tại [check_seeds.rs](file:///c:/Users/CBS/Documents/App/Farm/src-tauri/src/automation/check_seeds.rs). Giúp tệp tin kịch bản chính [buy_seeds.rs](file:///c:/Users/CBS/Documents/App/Farm/src-tauri/src/automation/buy_seeds.rs) cực kỳ tinh gọn, đồng thời giúp người dùng có thể dễ dàng xóa bỏ nhanh chóng hoặc thay đổi kịch bản chẩn đoán lỗi sau này mà không gây bất cứ ảnh hưởng nào đến logic auto-farm cốt lõi.
- **Thư viện tương tác dùng chung (Centralized Automation Helpers)**: Quy hoạch toàn bộ các hàm bổ trợ giả lập Win32 và chụp ảnh GPU (`click_ld`, `swipe_ld`, `capture_helper`, `check_and_clear_cancelled`) cùng các hằng số tỷ lệ màn hình (`BASE_W`, `BASE_H`) ra tệp tin dùng chung [utils.rs](file:///c:/Users/CBS/Documents/App/Farm/src-tauri/src/automation/utils.rs). Giúp các kịch bản auto-farm hiện tại và tương lai có thể nhập trực tiếp, tránh trùng lặp code và đạt độ chuẩn hóa kiến trúc cao.
- **Tối ưu hóa phản hồi dừng Check Lỗi (Diagnostic Scan Immediate Abort)**: Tích hợp điểm kiểm tra cờ hủy tức thời ngay bên trong vòng lặp so khớp tuần quy của hơn 20 ảnh mẫu hạt giống ở [check_seeds.rs](file:///c:/Users/CBS/Documents/App/Farm/src-tauri/src/automation/check_seeds.rs). Đảm bảo kịch bản chẩn đoán phản hồi và tắt luồng ngầm ngay lập tức khi người dùng nhấn nút dừng, triệt tiêu hoàn toàn độ trễ do chuỗi xử lý so khớp ảnh kéo dài.
- **Bù tọa độ điểm click Slider Max (Symmetric Slider Click Offset)**: Cải tiến công thức tính toán tọa độ click đối xứng để kéo Max thanh slider số lượng mua hạt giống tại [buy_seeds.rs](file:///c:/Users/CBS/Documents/App/Farm/src-tauri/src/automation/buy_seeds.rs). Bằng việc tự động cộng thêm một khoảng bù nhỏ là +5px (`BASE_W - slider_x + 5`), hệ thống đảm bảo điểm click chuột luôn nằm sâu và an toàn bên trong vùng Max, tăng độ tin cậy tuyệt đối cho quá trình thiết lập số lượng mua.
- **Quét động liên tục nút bán/mua trong 100ms (Dynamic Polling 100ms Timeout)**: Thiết lập vòng lặp quét liên tục nút `sell-produce.png` trong suốt thời hạn chờ 100ms sau khi click chọn hạt giống. Thay vì ngủ im lìm 100ms, kịch bản sẽ liên tục chụp/quét ảnh mỗi 10ms. Nếu tìm thấy nút sớm (ví dụ sau 15ms), hệ thống sẽ nhấp chuột ngay tức khắc để tiến hành các bước tiếp theo mà không cần chờ hết 100ms, còn nếu hết 100ms vẫn không thấy nút thì mới bỏ qua (Hết hàng). Vừa đảm bảo tốc độ cực đại vừa chống lag tuyệt đối.
- **Đo lường hiệu năng & Trích xuất log chi tiết (Detailed Profiling Diagnostics)**: Tạo riêng một hàm bổ trợ tập trung `log_time_diagnostics` tại [utils.rs](file:///c:/Users/CBS/Documents/App/Farm/src-tauri/src/automation/utils.rs) chuyên trách định dạng và phân tích thời gian tiêu thụ cho các bước (Chụp ảnh WGC, Chuẩn hóa kích thước, Quét ảnh mẫu, Click Win32). Cho phép người dùng dễ dàng tắt hoặc điều chỉnh các log chi tiết này ở một nơi duy nhất. Đồng thời, độ trễ click Win32 trong hàm `click_ld` và lệnh Test được tối ưu tuyệt đối xuống còn 0ms cố định (không tốn thời gian chờ giữ chuột).
- **So khớp siêu tốc & Phần trăm độ khớp (Fast Match & Similarity Percentage)**: Giữ nguyên thuật toán quét và trả về điểm khớp đầu tiên siêu tốc trong [recognize.rs](file:///c:/Users/CBS/Documents/App/Farm/src-tauri/src/core/recognize.rs), đồng thời quy đổi điểm Score SAD (0-255) thành tỷ lệ phần trăm độ khớp (%) trực quan in ra log lệnh Test để chẩn đoán chính xác.
- **Hệ thống Quét ảnh Đa biến thể Tự động (Generic Variant Engine)**: Thiết kế một hàm nhận diện siêu cấp `find_template_with_variants` đặt tại [utils.rs](file:///c:/Users/CBS/Documents/App/Farm/src-tauri/src/automation/utils.rs). Hệ thống tự động phân tách đường dẫn (như `"buttons/sell-produce.png"`) và tự động tìm mọi biến thể ảnh có trong thư mục (như `1_sell-produce.png`, `2_sell-produce.png`, v.v.) rồi quét song song. Cho phép thêm mới bất kỳ ảnh biến thể nào vào bộ cache mà không cần chỉnh sửa hay viết thêm bất kỳ một dòng code Rust nào, đảm bảo tính mở rộng linh hoạt tuyệt đối.
- **Đồng bộ hóa 3-Kênh Màu & Nâng cấp Test thử nghiệm Đa biến thể (Consistent RGB Channel Loading & Test Command Alignment)**: Khắc phục triệt độ lỗi mất đồng bộ RAM Cache do việc tải nhầm định dạng ảnh 4-kênh RGBA ở các tiến trình chạy ngầm khiến bộ nhận dạng `FastRecognizer` bị sai lệch chỉ số. Toàn bộ các tiến trình nạp RAM Cache của kịch bản mua và chẩn đoán được chuyển đổi đồng bộ 100% sang chuẩn 3-kênh RGB (`to_rgb8().into_raw()`). Đồng thời, nâng cấp lệnh Test thử nghiệm đơn lẻ trên UI tích hợp trực tiếp bộ máy quét đa biến thể `find_template_with_variants`. Đảm bảo quá trình kiểm tra thử bằng tay hoạt động chính xác tuyệt đối, hỗ trợ hoàn hảo cho mọi ảnh mẫu chứa tiền tố biến thể `1_`, `2_` một cách đồng bộ và thông minh.
- **Bắt ngoại lệ & Ghi nhận Log tự động trên Giao diện (Frontend Promise Rejection Catch & Auto-Logging)**: Sửa lỗi giao diện phản hồi im lặng và không ghi nhận log khi các lệnh Tauri trả về lỗi (`Err(String)`). Thiết lập bao bọc cơ chế xử lý tương tác của các nút bấm trên Frontend gồm **Capture**, **Test**, **Test All**, và **Check Lỗi** bằng các khối lệnh `try/catch` hoàn chỉnh. Đảm bảo mọi thông báo lỗi phản hồi từ GPU hay bộ so khớp của Backend đều được hiển thị trực quan 100% ra bảng điều khiển Console và tự động cập nhật trạng thái phím chức năng tương ứng thành "Lỗi" hoặc "Không thấy" một cách mượt mà.
- **Bộ nhận dạng Chữ số Mua hạt Siêu tốc không dùng OCR (High-speed OCR-free Seed Quantity Digit Recognition Engine)**: Thiết lập nạp tự động các tệp mẫu chữ số `0.png` đến `9.png` từ thư mục `templates/seeds/digits` vào bộ nhớ RAM Cache lúc khởi động. Tích hợp thuật toán nhận diện chữ số động `recognize_number_above_slider` dựa trên vùng quét thông tin cắt nhỏ (bounding box 120x25) nằm trực tiếp phía trên thanh trượt của hộp thoại mua. Thuật toán tự động tìm kiếm, định vị và sắp xếp các chữ số xuất hiện theo tọa độ ngang từ trái sang phải để ghép thành số lượng thực tế chính xác tuyệt đối (ví dụ: "10", "25"). Kết quả số lượng được ghi nhận theo thời gian thực và tự động thống kê tổng hợp số lượng hạt mua thành công của từng loại hạt ở dòng log kết thúc kịch bản mua một cách trực quan, chuyên nghiệp.
- **Công cụ Chẩn đoán nhanh Chữ số - Nút "Test Số" (Diagnostic Digit Matcher Button)**: Bổ sung thêm nút bấm chẩn đoán **"Test Số"** (màu chàm Indigo thanh lịch) ngay trên thanh tiêu đề của từng hàng LDPlayer. Cho phép người dùng chụp nhanh màn hình hiện tại để chạy thử và hiển thị trực tiếp con số lượng nhận diện được trên console log, giúp việc căn chỉnh ảnh mẫu chữ số cực kỳ trực quan và dễ dàng.
- **Tối ưu hóa bộ quét chữ số siêu tốc (Digit Recognition Threshold & Height Optimization)**: Chuẩn hóa ngưỡng nhận diện khớp hoàn hảo ở mức `24` giúp nhận diện chính xác 100% tất cả các số kép (như "13"). Đồng thời mở rộng vùng quét dọc lên `35` pixel (từ `slider_y - 45` đến `slider_y - 10`) để hỗ trợ hoàn hảo cho mọi tệp ảnh mẫu chữ số có chiều cao lớn mà không lo bị loại bỏ, đồng thời tối ưu hóa đĩa cứng bằng cách loại bỏ việc ghi đè file ảnh tạm.
- **Vòng lặp Chờ Động & Nhận Diện Tọa Độ Slider (Dynamic Active Waiting Loop & Coordinate Verification)**: Thay thế hoàn toàn thời gian chờ tĩnh 300ms truyền thống bằng vòng lặp chờ động thông minh giới hạn tối đa 200ms. Robot tự động phân tích tọa độ ngang `slider_x` của nút trượt:
  - Nếu `slider_x > 480` (trường hợp tồn kho chỉ còn 1 hạt, nút trượt nằm sẵn bên phải): Bỏ qua hoàn toàn việc click và chờ, nhảy thẳng đến nút Xác nhận (tiết kiệm thêm 300ms).
  - Nếu `slider_x <= 480` (trường hợp nhiều hạt): Gửi lệnh click chọn MAX và bắt đầu vòng lặp chụp nhanh 5ms để phát hiện ngay khi nút trượt cập bến bên phải, giúp kết thúc chờ và Xác nhận chỉ trong vòng 20ms-40ms thực tế.
- **Modal Cấu hình Mua hạt 2 Tab cao cấp (Premium 2-Tab Buy Seeds Modal)**: Tái cấu trúc hộp thoại cấu hình mua hạt giống thành giao diện 2 tab chuyên nghiệp: Tab 1 "Chọn hạt giống" để chọn loại hạt cần mua (đã lọc bỏ các tệp chữ số `seeds/digits/` dư thừa) và Tab 2 "Lịch sử mua" để theo dõi thống kê lượng hạt đã mua lũy kế thực tế được lưu trữ hoàn toàn trong bộ nhớ tạm RAM (In-Memory Session) mà không ghi đĩa cứng. Đồng thời bổ sung nút "Xóa lịch sử" kèm icon SVG Trash thời thượng để thiết lập lại bộ đếm của phiên làm việc bất cứ lúc nào. Giao diện chân trang footer cố định đối xứng tuyệt đối gồm 2 phím chức năng [Đóng] và [Lưu cấu hình hạt] giúp triệt tiêu hoàn toàn độ lệch nảy của chiều cao tab.
- **Tự động hóa chuẩn hóa kích thước cửa sổ (Integrated Auto-Resize on Refresh/Startup)**: Tích hợp hoàn toàn chức năng Resize cửa sổ LDPlayer về tỷ lệ vàng 960x540 trực tiếp vào tiến trình làm mới danh sách (Refresh). Xóa bỏ hoàn toàn nút bấm "Resize" thủ công ở từng dòng thiết bị giúp giao diện tinh gọn, thoáng đãng. Hệ thống tự động căn chỉnh chuẩn hóa tỷ lệ kích thước cho tất cả giả lập đang chạy ngay khi ứng dụng khởi động hoặc bất cứ khi nào người dùng bấm nút làm mới ở tiêu đề.
- **Tích hợp Vòng lặp Auto 5 Phút Thống Nhất (Unified Auto-Loop Run/Stop Button)**: Loại bỏ hoàn toàn ô check box "Auto 5p" thủ công. Gộp thẳng cơ chế chạy lặp lại sau mỗi 5 phút thành tính năng mặc định khi kích hoạt chạy kịch bản. Khi người dùng bấm **Chạy kịch bản**, nút bấm lập tức chuyển sang trạng thái hoạt động **Dừng kịch bản (màu đỏ)** bền vững trong suốt cả quá trình đang chạy lẫn thời gian chờ đếm ngược 5 phút tiếp theo, cho phép người dùng click chủ động để dừng kịch bản lập tức bất cứ lúc nào.
- **Tối ưu hóa Bố cục Một hàng cho Dòng Giả Lập (Single-Row Grid Header Optimization)**: Tái cấu trúc lưới CSS Grid hiển thị của mỗi hàng giả lập sang chuẩn 9 cột có kích thước thu gọn tối ưu. Phân bổ tỉ lệ hoàn hảo cho các thành phần (Info, Capture, Dropdown Chọn mẫu, Test, Test All, Check Lỗi, Test Số, và Settings). Thiết kế này giúp nút Settings (Gear icon) quay trở lại nằm thẳng hàng ngay bên phải cùng dòng, đảm bảo toàn bộ giao diện gọn gàng, cân đối 100% trên một hàng ngang duy nhất.
- **Dọn dẹp mã nguồn thừa (Cleaned Deprecated auto_run Property)**: Loại bỏ hoàn toàn trường cấu hình đã lỗi thời `"auto_run"` khỏi tệp `config.json`, cấu trúc dữ liệu Rust backend `AppConfig` (trong `persisted.rs`) và bộ khởi tạo Frontend ở `devices.js`. Giúp tối ưu dung lượng tệp cấu hình và triệt tiêu mã nguồn rác (anti-patterns) một cách chuyên nghiệp.
- **Bảng Quản lý Giả Lập Premium (Premium HTML Table Dashboard)**: Tái cấu trúc hoàn chỉnh giao diện danh sách thiết bị sang cấu trúc Bảng (`<table>`) chuyên nghiệp, trực quan với các cột: **LD** (Tên giả lập), **STATUS** (Trạng thái và Log tích hợp), **CONNECT** (Nút kết nối Session), **CHỨC NĂNG** (Dropdown chọn mẫu và bộ nút Test/Chụp màn hình/Cấu hình hạt/Cài đặt), và **THAO TÁC** (Phím chạy/dừng kịch bản). Toàn bộ các thông số và phím điều khiển được thu gọn trên 1 hàng ngang duy nhất, giúp giao diện tối giản tối đa mà cực kỳ trực quan và sang trọng.
- **Tối giản hóa Thao tác nhanh & Bỏ Shadow (Custom Action Dropdown & Shadowless Outline Aesthetic)**: 
  - Loại bỏ hoàn toàn đổ bóng viền (`box-shadow`) ở tất cả các header, bảng và hộp thoại để nhất quán 100% phong cách thiết kế phẳng "Premium Minimalist Outline".
  - Thu gọn toàn bộ các phím chức năng chẩn đoán cồng kềnh thành duy nhất một menu lựa chọn **Chức năng** (`.action-select`). 
  - Quy trình chạy cực kỳ thông minh và chuyên nghiệp: Menu lựa chọn đóng vai trò **Bộ chọn chức năng** (mặc định chọn sẵn `Chạy kịch bản` loop 5p). Khi người dùng chọn bất kỳ chức năng nào (Chụp màn hình, Test mẫu, Test All, Chẩn đoán lỗi, Nhận dạng số) và bấm nút **Play (▶️)** bên cột Thao tác, hệ thống sẽ ngay lập tức thực thi chức năng đó.
  - **Hộp thoại Cấu hình Tổng hợp Gộp 3-trong-1**: Tích hợp hoàn hảo cài đặt tham số nhận diện & điều khiển (Timeout, Delay nút, Độ nhạy khớp) vào chung một Modal cấu hình với Chọn hạt giống 🌱 và Lịch sử mua hạt giống ⏳. 
  - **Mở Cài đặt hệ thống làm mặc định**: Khi click nút bánh răng, Tab 1 ("Cài đặt chung") luôn tự động kích hoạt đầu tiên giúp dễ dàng tinh chỉnh, sau đó sang Tab 2 chọn hạt giống và Tab 3 xem lịch sử. Người dùng chỉ cần bấm **Lưu thiết lập** duy nhất một lần để đồng bộ lưu trữ.
  - **Cột THAO TÁC Siêu Tinh Gọn**: Loại bỏ nút cài đặt thừa, chỉ giữ lại đúng **2 nút bấm hình vuông hoàn hảo (`26x26px`)** trong cột Thao tác: Phím **Play/Pause** (`var(--accent-color)`) và phím **Cấu hình Tổng hợp (⚙️)** màu xám bạc chuyển màu hover thời thượng.
  - **Nút Chạy lại nhanh ứng dụng (⚡ - Restart App)**: Bổ sung nút bấm tia chớp siêu tốc ngay cạnh nút Làm mới (Refresh) trên thanh tiêu đề. Khi click, ứng dụng sẽ ngay lập tức tự khởi động lại trong tích tắc để nạp mã nguồn và giao diện mới nhất mà không cần mở thủ công.
  - **Nút Dừng/Pause màu Cam Vàng Đậm (Amber Orange - `#f59e0b`)**: Khi click chạy, phím sẽ mượt mà chuyển sang trạng thái dừng với màu cam vàng đậm đà, sang trọng, cực kỳ rõ nét và vô cùng dịu mắt (không bị chói gắt như màu đỏ cờ nguyên bản), tạo nên tổng thể cực kỳ hiện đại, đẳng cấp và đồng bộ.
- **Kịch bản Thu hoạch & Bán nông sản thông minh (Modular Harvest & Sell Script)**: Tích hợp kịch bản tự động Thu hoạch & Bán nông sản hoàn hảo độc lập tại [harvest_sell.rs](file:///c:/Users/CBS/Documents/App/Farm/src-tauri/src/automation/harvest_sell.rs). Kịch bản hỗ trợ tự động nhận diện và thoát khỏi modal mua hạt nếu đang mở bằng các cờ so khớp hình ảnh (`gift.png`, `close-seed.png`, `leave.png`, `leave2.png`), di chuyển về nhà (`home.png`), mở quản lý nhà (`open-house-manage.png`), mở modal thu hoạch (`open-harvest.png`), tự động lặp thu hoạch (`harvest.png`) và tự sửa lỗi túi đầy khi thiếu `confirm.png`. Tiếp theo di chuyển đến cửa hàng bán (`farm-shop.png`, `open-farm-shop.png`, `open-farm-shop2.png`), tự động chọn toàn bộ nông sản (`auto_pick.png`) và bán thông minh thích ứng hoàn hảo cho cả nông sản cấp cao (cần `confirm.png` + `ok.png`) và nông sản cấp thấp (chỉ cần `ok.png`). Giao diện Frontend điều khiển thông minh tự chạy ngay lần đầu tiên nhấn nút Play và thiết lập chu kỳ hẹn giờ lặp lại linh hoạt (tính bằng phút) theo thông số cấu hình chung trên giao diện.
- **Kích hoạt Kịch bản Hoạt động Linh hoạt (Active Script Selectors)**: Hỗ trợ hai hộp kiểm **"Kích hoạt Mua hạt"** và **"Kích hoạt Thu hoạch & Bán"** trực quan trong Tab 1 của Modal cấu hình, giúp người dùng tùy ý chọn bật/tắt từng kịch bản độc lập (mặc định mở cả hai, số lượt lặp thu hoạch và bán mặc định điều chỉnh về **2 lượt**). Trạng thái đếm ngược trên bảng điều khiển chính sẽ tự động điều chỉnh tương thích thời gian thực theo cấu hình đang chọn.
- **Tự động Cập nhật Cấu hình Thời gian thực (Dynamic Configuration Hot-Reloading)**: Nâng cấp luồng lặp tự động (Auto Loop Scheduler) trong devices.js để trực tiếp truy vấn biến cấu hình in-memory savedConfig mỗi giây một lần. Khi người dùng thay đổi trạng thái kích hoạt kịch bản (Mua hạt / Thu hoạch) trong Modal Thiết lập ⚙️ và nhấn Lưu, luồng chạy ngầm của bot sẽ ngay lập tức tự động thích ứng với cấu hình mới (tự kích hoạt/hủy đếm ngược, thay đổi chu kỳ đếm ngược động) thời gian thực mà hoàn toàn không yêu cầu người dùng phải dừng (Pause) rồi chạy lại (Play) thủ công.
- **Tối ưu hóa Bán Nông sản Siêu tốc & Đo lường chi tiết (Ultra-Fast Sell Sequence & Millisecond Profiling)**: Tối ưu hóa giới hạn thời gian chờ xuất hiện nút xác nhận bán (confirm.png/ok.png) từ 5 giây xuống đúng 1 giây. Nhờ đó, nếu balo đã bán sạch trái cây, bot sẽ phát hiện tức thì chỉ sau 1 giây, tự động đóng cửa hàng và kết thúc chu kỳ bán cực kỳ gọn gàng. Đồng thời tích hợp hệ thống log đo lường chi tiết từng mili-giây ([BAN DETAILED]) cho toàn bộ thao tác click bán 1, click bán 2, và đếm số khung hình quét xác nhận, giúp chẩn đoán hiệu năng siêu trực quan.
- **Nút Gạt Kết Nối Premium (Premium Session Toggle Switch)**: Thay thế hoàn toàn nhãn trạng thái "Ready / Connected" dạng badge truyền thống bằng nút gạt (Toggle Switch) thiết kế chuẩn Glassmorphism thời thượng. Khi kích hoạt kết nối thành công, nút gạt sẽ chuyển mượt mà sang màu xanh biển (var(--accent-color)) đồng bộ tuyệt đối với tone màu chủ đạo của ứng dụng ở cả hai chế độ Light và Dark Theme. Đi kèm hiệu ứng hoạt ảnh quét sáng chuyển động (Pulse Animation) cực kỳ bắt mắt khi đang trong tiến trình kết nối (Connecting), mang lại trải nghiệm chuyên nghiệp vượt trội.
- **Hệ thống Đăng nhập tự động & Vượt ải (Auto-Login & Safe Entry Recovery)**: Tích hợp mô-đun kiểm tra trạng thái game tự động khi bắt đầu mỗi kịch bản. Nếu game bị tắt (crash/closed), bot sẽ tự động thực hiện: Khởi chạy lại game bằng lệnh hệ thống -> Chờ khoảng thời gian cấu hình tùy chỉnh (Chờ mở game, mặc định 1 phút) để game load -> Tiến hành quét tìm nút điện thoại (phone.png) liên tục 1 giây một lần trong 30 giây -> Click mở điện thoại -> Nhận diện và click nút teleport về nhà (go-home.png) -> Chờ 20 giây tải sân vườn và bắt đầu farm. Tính năng này đi kèm nút lựa chọn bật/tắt (checkbox) và ô nhập thời gian chờ mở game linh hoạt trong Modal Thiết lập tổng hợp.
- **Bỏ qua Hội thoại NPC Siêu tốc (NPC Dialogue Rapid Bypass)**: Tự động phát hiện và tối ưu hóa thao tác tiếp cận NPC giao dịch (`open-farm-shop.png` và `open-seed-shop.png`) cũng như các thao tác thoát khỏi giao diện (`close-seed.png` và đóng shop `close-harvest.png`). Khi bot nhận diện và nhấp thành công vào các nút được chỉ định, hệ thống sẽ ngay lập tức tự động giả lập click **3 lần liên tiếp** tại chính xác tọa độ đó với khoảng trễ **300ms** giữa mỗi lượt nhấp. Quy trình này mô phỏng hành động click bỏ qua đối thoại của con người, giúp tua nhanh cuộc trò chuyện với NPC, mở/đóng cửa hàng tức thì mà không lo lắng về việc ảnh hưởng đến các nút xác nhận mua bán hay click nhầm vào các mô hình đằng sau bảng thu hoạch.

## 🛠️ Hướng dẫn sử dụng:
1. Mở LDPlayer (nên dùng LDPlayer 9).
2. Chạy ứng dụng OneFarm (Hệ thống sẽ tự động tìm kiếm giả lập và chuẩn hóa kích thước về 960x540 ngay khi mở).
3. Ấn nút **Ready** (Connected) để kích hoạt giả lập mong muốn.
4. Ấn nút **Cấu hình tổng hợp (⚙️)** để thiết lập tham số điều khiển và chọn hạt giống mong muốn mua, sau đó nhấn **Lưu thiết lập**.
5. Ấn **Play (▶️)** để kích hoạt kịch bản tự động mua hạt giống lặp lại mỗi 5 phút hoặc chọn chức năng đơn lẻ và chạy.
6. Trường hợp sửa đổi code hoặc cập nhật ứng dụng, chỉ cần ấn nút **Restart (⚡)** trên thanh tiêu đề để tự động khởi động lại tức thì.
7. Trường hợp mở thêm giả lập mới, chỉ cần ấn nút **Làm mới (Refresh icon)**, hệ thống sẽ tự động quét và chuẩn hóa kích thước cho thiết bị mới.

---
*Ghi chú: Luôn đảm bảo ldconsole.exe nằm đúng đường dẫn trong cấu hình.*

---

## 💾 Nhánh Sao Lưu Ổn Định & Khôi Phục (Backup & Revert)
* **Nhánh sao lưu phiên bản Tauri**: Toàn bộ mã nguồn giao diện Tauri ổn định hiện tại (tích hợp Auto-Login động, NPC Dialogue Bypass 3 clicks/300ms,...) đã được đẩy lên nhánh độc lập **`backup/tauri-dialogue-bypass`** trên GitHub.
* **Hướng dẫn khôi phục**: Trong trường hợp quá trình thử nghiệm giao diện siêu nhẹ mới gặp sự cố và bạn muốn quay trở lại phiên bản Tauri này, hãy làm theo các bước hướng dẫn chi tiết tại tệp [REVERT_INSTRUCTIONS.md](file:///c:/Users/CBS/Documents/App/Farm/REVERT_INSTRUCTIONS.md) để khôi phục đè hoàn toàn nhánh main một cách an toàn và tự động.

